@model SalePoint.Models.PaymentMethodModel

@{
    ViewBag.Title = "New";
}

@using (Html.BeginForm("Save", "PaymentMethod"))
{

    <h2>New</h2>

    <div class="row">
        @Html.HiddenFor(model => model.paymentMethodId)

        <div class="col-md-12">
            <h3 class="panel-title">Payment Method Description</h3>
            @Html.TextBoxFor(model => model.paymentMethodDescription, new { @class = "form-control", @required = "required", @maxlength = "20" })
        </div>
    </div>

    <p>
        <button class="btn-success">Save</button>
        @Html.ActionLink("Back", "Index")
    </p>

}